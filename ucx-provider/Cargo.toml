[package]
name        = "ucx-provider"
version     = "0.1.0"
edition     = "2021"
description = "UCX Provider Agent — hardware discovery, job runner, receipt emitter."

[dependencies]
ucx-protocol = { path = "../ucx-protocol" }
serde        = { workspace = true }
serde_json   = { workspace = true }
uuid         = { workspace = true }
thiserror    = { workspace = true }
chrono       = { workspace = true }
