[package]
name        = "ucx-protocol"
version     = "0.1.0"
edition     = "2021"
description = "Universal Compute Exchange — wire types, traits, error model. No I/O, no async, no ecosystem deps."

[dependencies]
serde      = { workspace = true }
serde_json = { workspace = true }
uuid       = { workspace = true }
thiserror  = { workspace = true }
sha2       = { workspace = true }
hex        = { workspace = true }
chrono     = { workspace = true }
