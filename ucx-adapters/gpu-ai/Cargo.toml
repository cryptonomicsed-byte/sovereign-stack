[package]
name        = "ucx-adapter-gpu-ai"
version     = "0.1.0"
edition     = "2021"
description = "UCX external provider adapter for GPU.ai (fine-tuning + inference)."

[dependencies]
ucx-protocol = { path = "../../ucx-protocol" }
serde_json   = { workspace = true }
uuid         = { workspace = true }
thiserror    = { workspace = true }
chrono       = { workspace = true }
reqwest      = { version = "0.12", features = ["blocking", "json"] }
